import "./index.css";
import Homepage from "./pages/homepage/Homepage";

function App() {
  // as we have only single page so we are returning home page only
  return <Homepage />;
}

export default App;
